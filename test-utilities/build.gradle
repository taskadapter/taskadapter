dependencies {
    compile project(':common')

    compile libraries.gson

    // yes, this is "compile", not "testCompile" dependency because "test-utilities" module
    // is itself referenced as "testCompile" dependency
    compile libraries.junit
    compile 'org.scalatest:scalatest_2.12:3.0.0'
}